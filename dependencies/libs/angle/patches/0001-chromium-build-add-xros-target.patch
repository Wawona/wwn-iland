diff --git a/build/config/apple/mobile_config.gni b/build/config/apple/mobile_config.gni
index 7d5be30..ed5f9b2 100644
--- a/build/config/apple/mobile_config.gni
+++ b/build/config/apple/mobile_config.gni
@@ -10,2 +10,2 @@ declare_args() {
-  # Valid values: "iphoneos" (default), "tvos", "watchos".
+  # Valid values: "iphoneos" (default), "tvos", "watchos", "xros".
   # Indicates the kind of iOS or iOS-based platform that is being targeted.
@@ -75,4 +75,5 @@ if (is_ios || is_watchos) {
     _target_platforms += [
       "iphoneos",
       "tvos",
+      "xros",
     ]
diff --git a/build/config/ios/ios_sdk.gni b/build/config/ios/ios_sdk.gni
index 6d15cc0..dd52fa4 100644
--- a/build/config/ios/ios_sdk.gni
+++ b/build/config/ios/ios_sdk.gni
@@ -80,4 +80,14 @@ if (ios_sdk_path == "") {
     } else {
       assert(false, "unsupported target_environment=$target_environment")
     }
+  } else if (target_platform == "xros") {
+    if (target_environment == "simulator") {
+      ios_sdk_name = "xrsimulator"
+      ios_sdk_platform = "XRSimulator"
+    } else if (target_environment == "device") {
+      ios_sdk_name = "xros"
+      ios_sdk_platform = "XROS"
+    } else {
+      assert(false, "unsupported target_environment=$target_environment")
+    }
   } else if (target_platform == "tvos") {
diff --git a/build/config/clang/BUILD.gn b/build/config/clang/BUILD.gn
index 126760a..01c522e 100644
--- a/build/config/clang/BUILD.gn
+++ b/build/config/clang/BUILD.gn
@@ -278,4 +278,12 @@ clang_lib("compiler_builtins") {
       } else {
         assert(false, "unsupported target_environment=$target_environment")
       }
+    } else if (target_platform == "xros") {
+      if (target_environment == "simulator") {
+        libname = "xrossim"
+      } else if (target_environment == "device") {
+        libname = "xros"
+      } else {
+        assert(false, "unsupported target_environment=$target_environment")
+      }
     } else if (target_platform == "tvos") {
diff --git a/build/config/rust.gni b/build/config/rust.gni
index 5500585..91eef8a 100644
--- a/build/config/rust.gni
+++ b/build/config/rust.gni
@@ -306,4 +306,14 @@ if (enable_rust) {
       } else {
         assert(false, "unsupported target_environment=$target_environment")
       }
+    } else if (target_platform == "xros") {
+      if (target_environment == "simulator") {
+        rust_abi_target = "aarch64-apple-visionos-sim"
+        cargo_target_abi = "sim"
+      } else if (target_environment == "device") {
+        rust_abi_target = "aarch64-apple-visionos"
+        cargo_target_abi = ""
+      } else {
+        assert(false, "unsupported target_environment=$target_environment")
+      }
     } else if (target_platform == "tvos") {
diff --git a/build/config/apple/sdk_info.py b/build/config/apple/sdk_info.py
index 09c4dca..94032d5 100755
--- a/build/config/apple/sdk_info.py
+++ b/build/config/apple/sdk_info.py
@@ -141,4 +141,6 @@ def Main():
                           'macosx',
                           'watchos',
                           'watchsimulator',
+                          'xros',
+                          'xrsimulator',
                       ])
diff --git a/build/config/ios/BUILD.gn b/build/config/ios/BUILD.gn
index a365faa..8d19ab1 100644
--- a/build/config/ios/BUILD.gn
+++ b/build/config/ios/BUILD.gn
@@ -46,5 +46,7 @@ config("compiler") {
   # OS.
   if (target_platform == "iphoneos") {
     triplet_os = "apple-ios"
+  } else if (target_platform == "xros") {
+    triplet_os = "apple-xros"
   } else if (target_platform == "tvos") {
     triplet_os = "apple-tvos"
diff --git a/build/config/ios/rules.gni b/build/config/ios/rules.gni
index f7a8f20..99fd6a1 100644
--- a/build/config/ios/rules.gni
+++ b/build/config/ios/rules.gni
@@ -98,6 +98,8 @@ template("ios_info_plist") {
     if (target_platform == "iphoneos") {
       _build_info_plist = "//build/config/ios/BuildInfo.plist"
+    } else if (target_platform == "xros") {
+      _build_info_plist = "//build/config/ios/BuildInfo.plist"
     } else if (target_platform == "tvos") {
       _build_info_plist = "//build/config/tvos/BuildInfo.plist"
     }
